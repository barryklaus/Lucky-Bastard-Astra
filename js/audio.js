'use strict';
LB.Audio={ctx:null,init(){if(!this.ctx){const AC=window.AudioContext||window.webkitAudioContext;if(AC)this.ctx=new AC()}if(this.ctx?.state==='suspended')this.ctx.resume()},tone(freq,dur,volume=.05,type='triangle',end=40){if(!LB.settings.sound||!this.ctx)return;const c=this.ctx,o=c.createOscillator(),g=c.createGain();o.type=type;o.frequency.setValueAtTime(freq,c.currentTime);o.frequency.exponentialRampToValueAtTime(Math.max(1,end),c.currentTime+dur);g.gain.setValueAtTime(volume,c.currentTime);g.gain.exponentialRampToValueAtTime(.001,c.currentTime+dur);o.connect(g);g.connect(c.destination);o.start();o.stop(c.currentTime+dur)},hit(type){this.tone(type==='metal'?490:type==='wood'?180:110,.13,.09,type==='metal'?'square':'triangle',35);if(type==='critical')this.tone(70,.35,.14,'sawtooth',25)},win(){this.tone(330,.2,.05,'square',440);setTimeout(()=>this.tone(440,.25,.04,'square',660),170)},click(){this.init();this.tone(680,.055,.025,'triangle',500)}};
